spring:
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/Dropbox/git-workspace/wxlund/cn-workshop-config/CN-Workshop   #Must point uri at your checkout of the repo on config branch. 
